# MongoDB Configuration
# Replace the values with your MongoDB Atlas credentials or local MongoDB instance
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_DB_NAME=asteroids

# Instructions:
# 1. Copy this file and rename it to .env
# 2. Replace the MONGODB_URI with your actual MongoDB connection string:
#    - For MongoDB Atlas: Get it from your cluster connection settings
#    - For local MongoDB: Use "mongodb://localhost:27017"
# 3. Set MONGODB_DB_NAME to your desired database name
#
# Note: The game will work locally without MongoDB configuration,
# but high scores won't be persisted between sessions. 